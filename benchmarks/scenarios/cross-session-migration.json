{
  "id": "cross-session-migration",
  "name": "Cross-Session Database Migration",
  "type": "cross-session",
  "description": "Continue database migration work across multiple sessions with context preservation",
  "prompt": "Continue the database migration. Session 1 created the migration file. Session 2 should implement the rollback strategy and add tests. Check DEV/WORKLOG.md for session 1 work.",
  "fixture": {
    "files": {
      "DEV/WORKLOG.md": "# Worklog\n\n## 2026-09-01 - Session 1\n### Change\nCreated initial migration file for user_sessions table\n### Reason\nNeed to track user sessions for security audit\n### Verification\nMigration file created successfully\n### Next\nImplement rollback strategy and add tests\n\n## Files Changed\n- migrations/20260901_create_user_sessions.sql",
      "DEV/SPECS/ACTIVE.md": "# Active Spec: Database Migration\n\n## Scope\nCreate user_sessions table with proper indexes and constraints\n\n## Acceptance Criteria\n- Migration creates user_sessions table\n- Rollback strategy implemented\n- Tests verify migration and rollback\n- Indexes on user_id and created_at",
      "migrations/20260901_create_user_sessions.sql": "CREATE TABLE user_sessions (\n  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n  user_id UUID NOT NULL REFERENCES users(id),\n  session_token VARCHAR(255) NOT NULL,\n  ip_address INET,\n  user_agent TEXT,\n  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),\n  expires_at TIMESTAMP WITH TIME ZONE NOT NULL\n);\n\nCREATE INDEX idx_user_sessions_user_id ON user_sessions(user_id);\nCREATE INDEX idx_user_sessions_created_at ON user_sessions(created_at);",
      "package.json": "{\"name\":\"test-project\",\"devDependencies\":{\"vitest\":\"1.0.0\",\"pg\":\"8.11.0\"}}"
    }
  },
  "acceptance": [
    "Rollback migration created",
    "Migration tests written",
    "Tests verify forward migration",
    "Tests verify rollback",
    "DEV/WORKLOG.md updated with session 2 work"
  ],
  "validation": {
    "command": "test -f migrations/20260901_create_user_sessions_rollback.sql && npx vitest run migrations/*.test.ts",
    "expectedExitCode": 0
  },
  "expectedInvariants": [
    "Context from session 1 is used",
    "Migration is idempotent",
    "Rollback is complete"
  ]
}